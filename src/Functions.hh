#ifndef INCLUDE_FUNCTIONS_HH
#define INCLUDE_FUNCTIONS_HH

#include <cmath>
#include <string>
#include <sstream>

using namespace std;

double chop (double x );
double Hermite(const int& n, const double& x);
int Factorial(int n);
double Squared (double x);
string intToString(int x);
double Free_Energy (double x);

#endif
